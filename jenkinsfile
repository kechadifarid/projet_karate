pipeline {
    agent {
        docker {
            image 'maven:ibmjava'
            args '-u root --entrypoint='
        }
    }
    stages {
        stage("lancer les tetst ") {
            steps {
                sh 'mvn clean install'
                sh 'mvn test'
            }
        }
    }
}